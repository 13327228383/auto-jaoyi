# Auto-Jaoyi 部署指南

> 目标读者：想把本项目跑起来（回测或实盘）的用户。
> 全文**不包含任何券商账号/密码**；同花顺客户端由你本人完成登录（程序只探测、不代登）。

---

## 0. 前置：免责与资质

- 本项目**仅供研究**，**不构成投资建议、不承诺收益**，使用者自负盈亏、自担合规责任。
- 实盘下单涉及你名下证券账户，运行前请理解所在辖区的交易/投顾规则，并在**模拟盘**上充分验证。

---

## 1. 环境总览（双 Python 架构）

本项目实盘是**决策与执行分离**的双进程架构，务必理解：

| 组件 | Python 位数 | 文件 | 职责 |
|---|---|---|---|
| 决策 (Decision) | **64-bit** | `auto_run.py` → `strategy_rotator.py` / `data_center.py` | 拉行情、算信号、定目标、风控 |
| 执行 (Executor) | **32-bit** | `exec32_trade.py` | 驱动**32位同花顺** xiadan.exe 下单 |

> **为什么 32-bit 执行端**：同花顺交易客户端是 32 位程序，GUI 自动化库（pywinauto/win32）需要与目标进程同一位数才能稳定识别控件。用 64 位 Python 驱动 32 位客户端会出现控件识别失败。因此必须坚持"决策64位 + 执行32位"，两进程通过**指令文件**通信。

---

## 2. 安装依赖

### 2.1 64-bit 决策环境

```bash
# 建议使用 pyenv 或 venv 建独立环境
pip install -r requirements_free.txt
```

### 2.2 32-bit 执行环境（关键，勿跳过）

确认是 **32 位 Python**（`python -c "import struct;print(struct.calcsize('P')*8)"` 应输出 `32`），再执行：

```bash
# 在 32 位 Python 里安装实盘驱动依赖
pip install pywinauto pywin32 ddddocr
```

> 32-bit 环境若出现 `UnicodeDecodeError`（tasklist 中文），本项目 exec32_trade.py 已内置 UTF-8 重配置，无需处理。

---

## 3. 同花顺客户端准备

程序**不会帮你登录券商/同花顺**，只探测客户端窗口是否已登录。故请先在桌面准备好客户端：

1. 安装并打开同花顺交易客户端 `xiadan.exe`（32 位，下单版）。
2. **登录时勾选「记住密码」**（否则下次开机不会自动登录，程序会一直等）。
3. 登录成功，保持客户端运行（程序会自动定位主窗口，无需手动点）。

> `config.ini` 里**不需要、也不应填**账号密码——已全部移除。只需将 `exe_path` 指向你本机 `xiadan.exe` 的真实路径。

---

## 4. 配置文件

```bash
# 复制模板并修正本机路径
cp config.example.ini config.ini
```

打开 `config.ini`，确认**唯一必填项** `[tonghuashun] exe_path` 指向你本机的同花顺下单程序完整路径（例如 `D:\AI\LICAI\同花顺\xiadan.exe`）。

其它如 `[mode] total_fund`（兜底资金，仅在读不到账户余额时用）可按需调整。

> **安全提醒**：配置文件不要提交到公开仓库。本项目已通过 `.gitignore` 排除 `config.ini`，仅提交脱敏模板 `config.example.ini`。

---

## 5. 运行回测（不上实盘，先验证）

```bash
# 运行的核心回测之一：单日亏损熔断时机对比
python research/backtest_daily_halt.py

# 其它常用：当前实盘策略收益率+保本率
python research/backtest_current.py

# 全部回测脚本见 research/ 目录，均使用免费真实历史行情
```

回测脚本会从 akshare/sina 拉取真实行情并缓存；首次运行需联网。

---

## 6. 运行实盘

```bash
# Windows 下双击，或命令行执行
start_auto.bat
```

`start_auto.bat` 会依次：
1. 启用防休眠代理 `keep_awake.py`（随时防止系统休眠/锁屏冻结鼠标点击）。
2. 记录参数复核提示 `review_hint.py`。
3. 前台运行 `auto_run.py`（决策循环 + 风控 + 触发 32 位执行端下单）。
4. 结束时关闭防休眠代理。

日志分两处：
- `auto_console.log` —— start_auto.bat 的控制台输出
- `auto_trade.log` —— auto_run.py 的交易/决策日志

> 首次运行建议**先只观察一个完整交易日日志**，确认对账、决策、风控均正常，再信任自动下单。
> 强烈建议先在**模拟盘**跑通全流程，再考虑真实账户。

---

## 7. 防火墙（可选但强烈建议）

实盘交易机应开启 Windows 防火墙：

```bash
# 以管理员身份运行
enable_firewall.bat
```

---

## 8. 常见问题（FAQ）

**Q1：程序一直提示"等待同花顺登录"？**
→ 客户端未登录或未勾"记住密码"。登录一次并勾选记住密码，保持客户端开着。

**Q2：为什么需要两个 Python？**
→ 决策 64 位、执行 32 位驱动 32 位客户端；混用会出现控件识别失败、下单异常。

**Q3：可以只回测、不实盘吗？**
→ 可以。只装 64 位决策依赖 + 跑 `research/*.py` 即可，不需要 32 位/同花顺客户端。

**Q4：回测数字很漂亮，能直接当预期吗？**
→ 不能。回测依赖历史牛市区间，方向可信、绝对值勿当真，务必在小额模拟盘充分验证。

**Q5：程序会不会乱动我的鼠标？**
→ 实盘下单/对账需要操作客户端。交易时段外及空仓时已做"静默"优化（跳过读账户、收口至15:00），正常时间内尽量减少弹窗与鼠标占用。